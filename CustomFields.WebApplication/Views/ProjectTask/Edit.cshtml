@using CustomFields
@using Domain
@model ProjectTaskViewModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>ProjectTask</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ProjectTask.Id" />

            <div class="form-group">
                <label asp-for="ProjectTask.Title" class="control-label"></label>
                <input asp-for="ProjectTask.Title" class="form-control" />
                <span asp-validation-for="ProjectTask.Title" class="text-danger"></span>
            </div>

            @Model.CustomFields.Render(nameof(ProjectTask), nameof(ProjectTask.CustomFields), Html, Model.ProjectTask.Id)

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

